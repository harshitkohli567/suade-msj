# Skill Registry data

Empty on purpose — this is Step 4 (Phase 1). The 11 Skills from
`Arbitration_docs_-_features_-_Skills.csv` will be encoded here as
`SkillRecord` objects (see `src/types/index.ts`) once section detection
(Step 3) exists for them to trigger against.
